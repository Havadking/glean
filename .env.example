# 复制成 .env 后填入真实密钥。.env 已在 .gitignore 里，不会提交。

# 当前默认 provider（config.yaml -> summarizer.api_key_env）
DEEPSEEK_API_KEY=

# 其他 provider 备用（config.yaml 里 summarizer.api_key_env 决定读哪个）
# Ollama 是本地模型，不需要 key
ANTHROPIC_API_KEY=
OPENAI_API_KEY=
DASHSCOPE_API_KEY=
MOONSHOT_API_KEY=
